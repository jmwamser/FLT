{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Address list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Streetnumber</th>
                <th>Streetname</th>
                <th>Neighborhood</th>
                <th>City</th>
                <th>Zip</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('addresses_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.streetNumber }}</td>
                <td>{{ entity.streetName }}</td>
                <td>{{ entity.neighborhood }}</td>
                <td>{{ entity.city }}</td>
                <td>{{ entity.zip }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('addresses_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('addresses_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('addresses_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
